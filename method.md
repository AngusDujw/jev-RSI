# Method：Jev 关节动作优化、方向测量与尝试复杂度

> 2026-09-25 · 理论框架与方法定义工作稿。
> 关联：[idea.md](idea.md)、[DISC-2026W39-001](Discussion.md)。当前仅有建模、标准数学推导与实验计划，没有机器人性能结果或已确立的新定理。

## 1. 方法定位

- **论文首要目标：**支撑问题定义与理论框架型论文（开坑型），建立定性方向信息、反馈动作生成与真实尝试成本之间的可检验关系；Jev 是首个实证对象。
- **研究对象：**Jev 提供粗粒度关节动作判断；外部 optimizer 类函数结合允许观测与执行历史，产生具体关节增量。
- **已确认范围：**静态可达位置目标、固定任务阶段、关节空间控制、动作执行后稳定再观测；先在仿真中建立可靠测量。
- **当前数学层级：**以标准平滑下降框架指导实验；余弦估计误差和 step 定义必须显式进入分析，实际算法的成本保证尚待建立。
- **未定项：**具体动作生成函数、是否随机、是否允许改变 Jev 的符号、实验资源、参数阈值及最终新颖贡献。
- **贡献调整：**依据实验证据与理论适用范围，按 [idea.md §5](idea.md#5-论文定位贡献层级与调整准则) 依次评估框架贡献、特定条件下的方法贡献、系统实证与边界贡献；每一层均需独立成立的新增内容。本文现有推导是框架起点。

## 2. 形式化定义

### 2.1 状态、目标与坐标

在局部可行关节域 U 中，用 m 个可控转动关节的角度 q∈R^m 表示状态；统一使用 rad 和欧氏范数，避开角度环绕歧义。若采用关节尺度矩阵或改变度量，必须同时变换梯度、方向及范数，不得只归一化其中一个量。

设静态目标为 x*∈R³，前向运动学位置为 f(q)，误差和目标函数定义为

\[
e(q)=f(q)-x^*,\quad V(q)=\tfrac12\|e(q)\|^2,\quad
J(q)=\frac{\partial f(q)}{\partial q},\quad g(q)=\nabla V(q)=J(q)^\top e(q).
\]

V 的单位为长度平方；成功阈值 V≤ε 对应位置半径 √(2ε)，并要求同一时刻满足规定的稳定条件。位置、姿态、接触和语义任务不是同一个目标函数；本版不为后三者给出收敛保证。

### 2.2 Jev 判断与动作函数

采用候选离散接口：每个关节的 Choice 为 {−1,0,+1}，表示其他关节暂时不动时的小幅负转、保持、正转。实现前需核验控制代码与此语义一致。

记状态文本／结构化观测为 o_t，模型输出为

\[
p_t=\operatorname{Jev}(o_t),\quad p_{t,j}=(p_{t,j}^{-},p_{t,j}^{0},p_{t,j}^{+}),
\quad s_{t,j}=\arg\max_{a\in\{-1,0,+1\}}p_{t,j}^{a}.
\]

平局规则需在实现前固定；概率索引与符号需显式映射。每个问题的概率分布不是梯度，也不自动构成关节间联合分布。并行回答不保证组合动作协调，confidence 不等于控制正确率。[官方 Choice](https://docs.typesafe.ai/primitives/choice)、[多问题语义](https://docs.typesafe.ai/primitives)、[Confidence](https://docs.typesafe.ai/confidence)。

定义动作生成分布或函数

\[
u_t\sim Q_\phi(\cdot\mid q_t,e_t,p_t,\mathcal H_t),\qquad
q_{t+1}=q_t+u_t+r_t.
\]

H_t 为采样前的可用执行历史；u_t 是命令关节增量，r_t 是实际最终位置相对命令的跟踪偏差。确定性函数对应集中在单个动作上的分布；φ 不要求神经网络参数。Q 只能使用声明的观测，不能偷偷读取诊断梯度。

分别记录三种非零单位方向：模型 d^J=s/‖s‖、生成 d^Q=u/‖u‖、实际 d^E=(q_{t+1}−q_t)/‖q_{t+1}−q_t‖。全 hold、零命令和零实际位移单列为停滞情形，不伪造余弦或从主成本统计中删除。

### 2.3 连续控制与 step

| 符号／计数 | 严格含义 |
|---|---|
| δt_sim | 仿真内部积分时间步，与一次 Jev 判断不同 |
| Δt_t | 相邻外层观测／决策时刻之间的实际时长 |
| α_t=‖u_t‖ | 命令动作幅度，单位 rad，不是时间 |
| N | 外层尝试次数，包含没有有效进展的尝试 |
| C_API | 所有实际模型请求；批量问题数另记 |
| C_exec / C_probe | 正式动作／额外试探，另记候选评估与复位 |

一次外层尝试为“观测 → Jev 判断 → 生成并执行受限动作 → 达到稳定条件后观测”。稳定需预先定义速度阈值、持续时间、跟踪容差和最长等待；超时是失败或协议规定的异常，不能当作稳定样本。

静态目标和执行后稳定使 q 上的局部分析具有可检验的适用范围，但不令 r_t 自动为零。实际周期可因模型延迟和稳定等待而变化，必须记录；只有被协议实现的固定周期才能称固定 Δt。模型等待期间采用何种保持控制也需记录。

如果未稳定就持续决策、存在显著惯性／接触切换或目标运动，应改用完整状态 z=(q,qdot,其他动态状态) 和转移 z_{t+1}=Φ_{Δt_t}(z_t,u_t)，不能直接沿用本版静态保证。旧观测上的方向证书也不能自动用于延迟后已经变化的状态。

### 2.4 梯度余弦及“对齐率”

当 g≠0、‖d‖=1 时，定义

\[
c(q,d)=-\frac{g(q)^\top d}{\|g(q)\|}\in[-1,1].
\]

c 是对齐程度；P(c>0) 是方向正确比例；给定 γ>0 时 P(c≥γ) 是达到指定强度的比例；测试集平均值、分位数与全轨迹统一下界 ρ 是不同对象。

实验分别报告 d^J、d^Q、d^E 的 c；若 Q 改变了方向，不把最后的好余弦全部归功于 Jev。近零梯度会使比值不可靠，应报告剔除条件、数量和覆盖率；这些试验仍保留在成功和成本统计中。

概率理论的候选条件应表述为 P(c_t≥γ | F_t)≥p 等，F_t 包含动作采样前历史和状态；随机性可能来自 Q 或模型，必须指明来源。测试状态上的总体比例不能直接证明此条件。[概率下降方向的定义](https://optimization-online.org/wp-content/uploads/2014/03/4284.pdf)。

### 2.5 梯度的测量与误差

**路径 A：仿真参考梯度。** 对无接触的静态位置目标，利用正确更新状态后的解析运动学 Jacobian 计算 g=Jᵀe；只截取对应可控关节列，保持目标、坐标系、角度单位一致。[MuJoCo Jacobian 接口](https://mujoco.readthedocs.io/en/stable/APIreference/APIfunctions.html#mj-jacsite)。它是仿真模型下的参考，不等于真实机械臂的精确梯度。

**路径 B：中央差分。** 在同一局部状态、同一目标下：

\[
\widehat g_j(q)=\frac{\widetilde V(q+h e_j)-\widetilde V(q-h e_j)}{2h},\qquad j=1,\ldots,m.
\]

e_j 此处为第 j 个坐标基向量，不是任务误差 e(q)。每个状态需 2m 次函数评估；若通过动态执行测量，还需复位完整相关状态、稳定等待和重复测量，全部记录为诊断开销。

若各坐标扰动线段上的三阶导绝对值≤M，且每次固定位置函数观测满足 |Vtilde−V|≤σ，则

\[
|\widehat g_j-g_j|\le Mh^2/6+\sigma/h,\qquad
\|\widehat g-g\|\le b(h):=\sqrt m\,(Mh^2/6+\sigma/h).
\]

这是**绝对有界观测误差**条件，不是把标准差 σ 代入即成立。定位误差需通过状态偏差及局部变化界另行处理；无界随机噪声需高概率分析。多尺度 h 检查可以发现不稳定，但仅凭曲线平稳不能宣称已获得严格 M、σ 上界。[Berahas 等，中央差分误差界](https://optimization-online.org/wp-content/uploads/2019/05/7189.pdf)。

### 2.6 带误差的余弦认证

设 ‖ghat−g‖≤b、‖d‖=1。若 A=−ghatᵀd−b>0，则

\[
c(q,d)\ge\underline c(q,d):=
\frac{-\widehat g^\top d-b}{\|\widehat g\|+b}>0.
\]

证明：−gᵀd≥−ghatᵀd−b=A>0，且 ‖g‖≤‖ghat‖+b，相除即可；A>0 也保证 g≠0。若 A≤0，只能说无法认证正对齐，不能把下界强行设成 0 或断言方向错误。

若 b 仅以概率 1−δ 成立，证书也仅在该事件上成立；多个时刻联合保证需另行分配失败概率。有限状态上的最小认证值仅描述这些状态，不成为整个闭环域的统一 ρ。

### 2.7 成功、预算与统计对象

Tε 为第一次同时达到 V≤ε 和稳定条件的外层尝试编号；初始已成功则 Tε=0，没有到达则 Tε=∞。预算 B 停止意味着该轨迹被截断／判为预算内失败，不能把 B 宣称为理论最大成功次数。

主评价报告 S(n)=P(Tε≤n)；达到指定成功率的预算 N(p₀)=inf{n:S(n)≥p₀} 的阈值尚待确认。模型请求、物理执行、候选计算、复位与墙钟时间分别报告；理论中的更新次数只有在已证明固定映射后才能转换成请求数或时间。

## 3. 关键结论与推导边界

以下给出构建框架所用的标准工具与条件式推导；其新增内容、可实现条件和对闭环行为的预测能力仍需建立。

### 3.1 单步下降与采样代价

若 V 在整个动作线段上 L-smooth，理想无跟踪误差更新 q+=q+αd，‖d‖=1，则

\[
V(q+\alpha d)\le V(q)+\alpha g^\top d+\tfrac L2\alpha^2
=V(q)-\alpha\|g\|c+\tfrac L2\alpha^2.
\]

对于条件随机动作 u，要求其支撑对应的动作线段都在同一有效域内，且有有限二阶矩：

\[
\mathbb E[V(q+u)\mid\mathcal F]
\le V(q)+g^\top\mathbb E[u\mid\mathcal F]
+\tfrac L2\mathbb E[\|u\|^2\mid\mathcal F].
\]

均值方向的收益和动作二阶矩的代价需一起分析。相同均值下增加采样方差，会增大这个保守界中的二阶项；这不证明随机采样一定更差，也不允许预设它更好。选多个候选后再执行时，应分析选择后的分布，同时计入全部候选评估成本。

### 3.2 理想情况下的更新次数上界

仅在下列条件对所有终止前状态及更新线段统一成立时使用：

- A1：目标可达且 V*=0，V 为 L-smooth。
- A2：局部 PL 条件 ‖g_t‖²≥2μV_t，μ>0；排除目标误差仍大而梯度退化的情形。
- A3：每步非零方向满足 c_t≥ρ>0，不存在额外 hold 或停滞尝试。
- A4：理想步长 α_t=c_t‖g_t‖/L 可执行，不触碰动作限幅或关节边界，r_t=0，轨迹留在有效域。
- A5：每次理想更新均在最长等待期限内达到统一稳定条件；一次更新恰对应一次已计数外层尝试，没有额外重试、探测或失败等待。

代入 §3.1：

\[
V_{t+1}\le V_t-\frac{c_t^2\|g_t\|^2}{2L}
\le(1-\chi)V_t,\qquad \chi=\mu\rho^2/L.
\]

对 V₀>ε>0、0<χ<1，反复递推得

\[
T_\varepsilon\le
\left\lceil\frac{\log(V_0/\varepsilon)}{-\log(1-\chi)}\right\rceil.
\]

初始已成功时 Tε=0。不可达目标不能照抄此式；χ=1 是单步达到理想零误差的边界情形，需单列。

**限制：**α 使用了未知梯度和曲率；本式是解释方向质量作用的理想基准，尚不是 Qφ 的保证。没有 A5 时，公式只能限制理想几何更新数，不能直接限制 §2.7 的真实成功尝试数 Tε。实验平均 c 不能替代 ρ；L、μ 也不能由少数观测的极值直接宣布为统一常数。命题给的是充分更新次数上界，不是最优下界，也不保证两个上界之比等于真实加速倍数。

### 3.3 校准误差如何进入理想关系

作为 §3.2 的扩展推导，设 a_t=−g_tᵀd_t>0，校准估计满足 |ahat_t−a_t|≤ζa_t，0≤ζ<1。若使用可行 α_t=ahat_t/L，其他理想条件保持，则

\[
V_{t+1}\le V_t-\frac{a_t^2-(\widehat a_t-a_t)^2}{2L}
\le\left(1-\frac{\mu\rho^2(1-\zeta^2)}L\right)V_t.
\]

这说明校准误差会削弱充分收缩率；如何以有限成本获得 ζ 的保证，尤其接近目标时，仍是研究问题。它不规定实际 optimizer 必须使用这一估计器。

### 3.4 跟踪误差不能省略

若实际更新为 q+=q+αd+r，且有明确 ‖r‖≤η，整个实际线段仍满足同一光滑界，则

\[
V(q^+)\le V(q)-\alpha a+\|g\|\eta+\tfrac L2(\alpha+\eta)^2.
\]

推导是将 gᵀr≤‖g‖η 和 ‖αd+r‖≤α+η 代入下降引理。只有误差项足够小，才保留净下降；写出 r 而不给任何界，不算处理了连续控制。若稳定等待／延迟使状态变化，需在实际动作起始状态评价方向，或单独界定这部分变化。

### 3.5 最小次数与最大次数的区别

若每次外层尝试内末端净位移不超过 D_max，且尝试之间没有未计入的移动，初始位置距离为 R₀，则由三角不等式：

\[
T_\varepsilon\ge
\left\lceil\frac{(R_0-\sqrt{2\varepsilon})_+}{D_{\max}}\right\rceil.
\]

这是物理预算下界，未必紧、未必可达。如果只知道末端速度≤v_max，可以先得到运动时间下界 (R₀−√(2ε))_+/v_max；要变成次数，必须再约束每次动作的持续时间。固定周期 Δt 时可取 D_max=v_maxΔt；可变周期不能无说明地套固定周期公式。

上述下界不依靠 Jev 的平均余弦；仅凭余弦无法推出非平凡的最小尝试次数。若允许任意长的错误动作序列，有限确定性最大次数也不成立，应研究期望或高概率上界，并明确条件历史、失败动作代价和执行规则。这些概率界尚未在本项目建立。

### 3.6 可证伪反例

即使方向每次正确，固定幅度也可能停滞：V(x)=x²/2，x₀=h/2，执行 x+=x−h sign(x)，则轨迹在 ±h/2 之间往返，V 恒为 h²/8。因此 ε<h²/8 时没有有限成功次数；它反驳“只要方向对，就能任意精度到达”的无条件主张。

## 4. 如何用理论指导方法与实验

1. 先确认 g 的参考与误差，再评价方向；不要同时把未知梯度、未知执行误差和模型判断揉成一个成功率。
2. 几何诊断分别使用 d^J、d^Q、d^E；对齐改进来自哪个模块应可追踪。
3. 用实际单步进展 V_t−V_{t+1} 检查小动作近似；可记录 κ_t=(V_t−V_{t+1})/V_t，但经验 κ 的极值也不自动构成统一收缩率。
4. 先用固定幅度与简单反馈调幅建立有效对照，再决定采样是否值得。未经明确决定，不固定 Q 的函数类、是否改符号或具体更新系数。
5. 梯度／曲率真值只供评价或标明额外信息的对照；若方法开始使用差分探测，探测、复位、等待和额外模型请求都进入算法成本。
6. 解析梯度与差分的对应、条件式上界对轨迹的预测力、考虑真实成本后的收益，都可能失败；失败应按相应范围记录，而非选择性删掉状态。

## 5. 与实验日志和执行任务对齐

完整实验流程见 [idea.md §4.2](idea.md#42-实验路线前置验证与两个主实验)。先完成仿真环境、Jev 信息有效性与最小闭环 feasibility 验证，再开展两个主实验；当前各阶段均尚无通过结论。理论推导可并行，失败诊断不等同于扩大收益比较。

| 阶段 | 执行同学的交付物 | 进入下一步前检查 |
|---|---|---|
| 前置 0A：仿真环境与测量链路 | 接口／单位映射、完整复位与执行误差、稳定与超时记录、基础梯度交叉校验、正对照轨迹 | 达到预设容差；排除未解释的环境与测量错误；额外诊断信息不流入 Jev |
| 前置 0B：Jev 有效性 | 输出可用性、单关节及联合方向质量、实际单步进展、无信息／打乱输出候选消融、效应与不确定性 | 测量可信；观测权限与幅度／可行性可比；接口成功与方向有用分开判定，保留全 hold 和失败 |
| 前置 0C：闭环 feasibility | 简单动作规则下多起点完整轨迹、到达与超时事件、全部成本及失败分类 | 预设精度、可靠性与预算要求下可复现；单条成功演示不算通过；失败先诊断方向、尺度与执行环节 |
| 主实验一：量尺与机制 | 扩展状态上的解析／差分梯度、扰动尺度曲线、余弦误差与覆盖率、方向和幅度响应 | 基本测量已校验；参考误差小于被讨论信号；原始概率和无效样本完整保留 |
| 主实验二：闭环理论与成本 | 同起点对照轨迹、实际时序、跟踪误差、进展预测与成功—成本曲线 | 前置可行性具备依据；评测集与规则冻结；成功／稳定标准相同；无隐藏复位、oracle 或漏记成本 |

各阶段数值门槛、样本量及候选规则在运行前确认；开发和前置验证数据不替代主实验冻结评测。前置验证中的经验余弦或成功率不能代替 §3 的统一假设，也不自动成为理论证明。

最小记录字段：trial_id、状态种子／版本、q 与可用速度、目标及末端位置、观测和调用／执行／稳定时间戳、完整 Jev 请求与返回版本和概率、s、u、实际 Δq、参考梯度及来源、h／噪声与误差界依据、各类 c、V 前后值、所有成本计数、失败／超时原因。观测丢失和全 hold 也留记录。

- [EXP-2026W39-002](LOGS/2026-W39.md#exp-2026w39-002)：经用户授权的文档落盘与公式审阅，非性能验证。
- [EXP-2026W39-003](LOGS/2026-W39.md#exp-2026w39-003)：研究纲领与贡献层级的一致性审阅，非性能验证。
- [EXP-2026W39-004](LOGS/2026-W39.md#exp-2026w39-004)：前置验证与主实验依赖关系的文档审阅，非控制实验。

## 6. Changelog（公式级变更）

| 日期 | 改动摘要 | 触发原因 | 关联议题 | 关联实验 | commit | 操作人 |
|---|---|---|---|---|---|---|
| 2026-09-24 | 首次定义 q／V／Q、三类方向、step 与成本；写入差分误差、余弦认证、理想收缩、校准与跟踪误差项、物理下界及限制 | 用户逐轮确认关节控制、静态稳定观测边界，并明确要求写入 idea.md 与 method.md | DISC-2026W39-001 | EXP-2026W39-002（文档审阅） | no-git | Agent @Codex，经 User 授权 |
| 2026-09-24 | 对齐开坑型理论框架的首要定位、实证驱动的贡献收缩准则及正式表述；公式、假设与控制范围保持一致 | 研究负责人明确论文目标和纲领性文档要求 | DISC-2026W39-001 | EXP-2026W39-003（文档审阅） | no-git | Agent @Codex，经 User 授权 |
| 2026-09-25 | 补齐环境验收、Jev 有效性和闭环 feasibility 的前置流程及其与主实验的依赖；公式与假设未改动 | 补全研究纲领中缺少的前置检验 | DISC-2026W39-001 | EXP-2026W39-004（文档审阅） | no-git | Agent @Codex |
